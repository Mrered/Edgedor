---
phase: quick
plan: 260827-0d0-adaptive-editor-behavior
subsystem: editor-ui
tags: [svelte, tauri, monaco, session, macos-panel]
requires:
  - phase: existing-edgedor-workspace
    provides: Monaco 编辑器、会话恢复和原生边缘面板桥接
provides:
  - 按呼出边缘自适应的侧边标签位置
  - 临时标签自动语言检测与手动选择锁定
  - 可隐藏且无残留网格的标签栏
  - 规范化快捷键冲突检查和跨标签全量搜索
affects: [panel-layout, editor-language, session-settings, cross-tab-search]
tech-stack:
  added: []
  patterns: [可选原生状态字段在无值时省略, 旧设置通过默认值归一化]
key-files:
  created: []
  modified:
    - src-tauri/src/lib.rs
    - src-tauri/src/native_panel.rs
    - src/lib/tauri/panel.ts
    - src/lib/session/model.ts
    - src/lib/i18n.ts
    - src/routes/+page.svelte
key-decisions:
  - "自动语言检测仅作用于临时标签，不覆盖真实文件的语言模式。"
  - "隐藏标签栏时同时移除 side-tabs 布局类，让工作区完整占位。"
patterns-established:
  - "呼出边状态：只在边缘呼出事件中携带 triggerEdge，隐藏或普通显示后清空。"
  - "搜索结果：每个标签最多收集 100 个匹配，并使用标签 ID 与偏移量构造唯一键。"
requirements-completed: []
duration: 10min
completed: 2026-08-27
---

# Quick Task 260827-0d0：自适应编辑器行为补齐摘要

**边缘呼出的标签布局、临时内容语言检测、标签栏隐藏、快捷键冲突防护与跨标签全量搜索已连成可编译的完整行为。**

## 性能

- **耗时：** 10 分钟
- **开始：** 2026-08-26T16:18:00Z
- **完成：** 2026-08-26T16:28:21Z
- **任务：** 3
- **修改文件：** 6

## 完成内容

- 原生面板在左右边缘呼出时向前端传递 `triggerEdge`，隐藏或非边缘显示后清除，侧边标签自动移到呼出边的反侧。
- 临时标签根据内容检测 shell、Rust、Python、Go、HTML、JSON、JavaScript/TypeScript 和 Markdown；手动选择后不再自动覆盖。
- `showTabs` 以 `true` 兼容恢复旧设置，隐藏标签栏时不保留侧栏网格空列。
- 自定义快捷键按别名、大小写和修饰键顺序规范化后检查冲突；跨标签搜索返回每标签最多 100 个匹配。

## 任务提交

1. **任务 1–3：自适应编辑器行为补齐** - `0ba7f53` (`feat`)

规划文档依据父执行器要求未纳入该源代码提交。

## 文件变更

- `src-tauri/src/lib.rs` - 定义可选呼出边状态，并在普通显示或隐藏时清空。
- `src-tauri/src/native_panel.rs` - 在边缘呼出事件中发送呼出边，隐藏事件不再发送空值字段。
- `src/lib/tauri/panel.ts` - 为前端面板状态增加受限的可选 `triggerEdge` 类型。
- `src/lib/session/model.ts` - 增加手动语言选择标记与 `showTabs` 默认恢复。
- `src/lib/i18n.ts` - 增加标签栏与快捷键冲突的中英文文案。
- `src/routes/+page.svelte` - 连接语言检测、布局、设置、冲突检查与多匹配搜索行为。

## 决策

- 自动语言检测只修改临时标签，保留真实文件和预览标签已有语言类型。
- Rust `None` 不序列化为 `null`，与 TypeScript 的可选字段契约保持一致。

## 计划偏差

### 自动修复问题

**1. [Rule 1 - 缺陷] 修复同一标签多个搜索结果的重复键**
- **发现于：** 任务 3
- **问题：** 所有结果原本只使用标签 ID 作为 Svelte key，同一标签多个匹配会产生重复键。
- **修复：** 使用标签 ID 和匹配偏移量组合唯一键。
- **文件：** `src/routes/+page.svelte`
- **验证：** `npm run check` 和 `npm run build` 通过。
- **提交：** `0ba7f53`

---

**总偏差：** 1 项自动修复（1 项缺陷）。
**影响：** 修复是跨标签全量匹配正确渲染的必要条件，未扩大功能范围。

## 验证

- `git diff --check` 通过。
- `npm run check` 通过，0 错误、0 警告。
- `npm run check:session` 通过。
- `npm run build` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过，3 项测试全部通过。
- 额外旧设置检查确认缺少 `showTabs` 时回退为 `true`。

## 已知占位

无。

## 威胁标记

无新的网络、身份验证、文件访问或模式边界。

## 用户配置

无。

## 后续准备

- 源代码已通过完整自动验证，可继续后续面板动画、窗口层级或编辑器分区任务。
- 本任务无阻塞项。

## 自检：通过

- 6 个修改源文件与本摘要文件均存在。
- 提交 `0ba7f53` 已在 Git 历史中确认。
- 新增代码未检出 TODO、FIXME 或占位文案。

---
*Quick Task: 260827-0d0-adaptive-editor-behavior*
*Completed: 2026-08-27*
