---
quick: 260827-0d0-adaptive-editor-behavior
status: planned
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/native_panel.rs
  - src/lib/tauri/panel.ts
  - src/lib/session/model.ts
  - src/lib/i18n.ts
  - src/routes/+page.svelte
---

# 自适应编辑器行为补齐

## 目标

完成工作区中已经开始的自适应侧边标签、临时内容自动语言检测、标签栏显示开关、自定义快捷键冲突检查和跨标签全部匹配搜索，并保持旧会话数据可恢复、原生面板状态与前端布局一致。

## 任务

### 任务 1：打通触发边状态并自适应侧边标签

**files:** `src-tauri/src/lib.rs`、`src-tauri/src/native_panel.rs`、`src/lib/tauri/panel.ts`、`src/routes/+page.svelte`

**action:** 完成 `trigger_edge`/`triggerEdge` 从原生边缘触发事件到前端 `PanelStatus` 的类型与事件桥接；左边呼出时把侧边标签放到右侧，右边呼出时放到左侧，非边缘触发时继续使用用户保存的标签布局。隐藏或失焦事件应清除触发边，避免旧边缘状态污染后续显示。

**verify:** `<automated>npm run check && cargo check --manifest-path src-tauri/Cargo.toml</automated>`；检查 Rust 序列化字段与 TypeScript 的 `triggerEdge?: 'left' | 'right'` 一致。

**done:** 左右边缘呼出分别得到相反侧标签布局，隐藏后状态清空，前端与 Rust 均通过类型和编译检查。

### 任务 2：补齐自动语言检测与可隐藏标签栏

**files:** `src/lib/session/model.ts`、`src/lib/i18n.ts`、`src/routes/+page.svelte`

**action:** 为未手动指定语言的临时内容按内容特征检测常见语言，用户从状态栏手动选择后停止自动覆盖；为设置增加 `showTabs` 默认值、归一化和中英文文案。旧会话缺少 `languageManuallySelected` 或 `showTabs` 时必须按兼容默认值恢复。标签栏关闭时不要给根布局保留 `side-tabs`/`side-tabs-right` 网格类，确保工作区占满原标签栏位置，不留下空白网格列。

**verify:** `<automated>npm run check && npm run check:session</automated>`；用旧格式会话数据确认 `showTabs` 回退为 `true`，并检查顶部、左侧、右侧布局在隐藏标签栏后均不保留空列。

**done:** 临时文本可自动识别 shell、Rust、Python、Go、HTML、JSON、JavaScript/TypeScript、Markdown，手动语言选择保持稳定；旧会话正常恢复；隐藏标签栏后编辑区完整占位。

### 任务 3：完善快捷键冲突与跨标签全部匹配搜索

**files:** `src/lib/i18n.ts`、`src/routes/+page.svelte`

**action:** 保存自定义快捷键前以大小写不敏感的规范化绑定检查其他命令，冲突时显示本地化提示并恢复输入框原值；跨标签搜索遍历每个标签的全部匹配项，为单标签设置明确上限以避免超大内容阻塞，并为结果数组声明确定类型，避免 Svelte/TypeScript 推断为 `never[]`。保留点击结果后聚焦对应标签的现有行为。

**verify:** `<automated>git diff --check && npm run check && npm run check:session && npm run build && cargo test --manifest-path src-tauri/Cargo.toml</automated>`；人工冒烟确认同一标签多次匹配均出现、不同标签结果均可跳转、重复快捷键不会覆盖已有绑定。

**done:** 冲突快捷键不会写入设置，跨标签搜索返回每个标签的全部受限匹配，前端构建、会话检查、Rust 测试和差异格式检查全部通过。

